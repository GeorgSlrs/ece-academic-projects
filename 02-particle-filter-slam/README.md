# Particle Filter-Based SLAM

Team coursework on differential-drive robot localization and mapping using particle-filter / FastSLAM concepts, noisy sensing, resampling, obstacle navigation, and line-based map reconstruction.

The public version contains a cleaned source export of the main simulation notebook. Large local map assets and generated notebook outputs were intentionally omitted.
